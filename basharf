#include <iostream>
#include <cstring>
#include <cstdio>
using namespace std;

void BasıBuyuk(int, char[]);

int main(){
    char s[50];
    gets(s);
    cout<< s[0];
    for(int i=0; i<strlen(s); i++){
        if(s[i]==' ') {
            cout << s[i+1];
        }
    }
}
